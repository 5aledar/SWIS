import Form from "@/features/auth/components/Form";

const Login = () => {
  return (
    <div className="flex justify-center bg-slate-100 items-center w-full">
      <Form />
    </div>
  );
};

export default Login;
